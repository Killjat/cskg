# API Hunter 🔍

**API Hunter** 是一个专业的网页API接口发现工具，通过深度爬虫技术自动发现和分析网站中的API接口。

## 🎯 为什么需要 API Hunter？

在现代网络安全领域，API已成为最重要但也最容易被忽视的攻击面。我们的实际测试显示：
- 从98个真实网站中发现了16个API端点
- 包括企业管理系统、财务数据、认证接口等敏感API
- 90%的API端点未被传统扫描工具发现

**详细了解**: 
- [为什么要开发API Hunter？](WHY_API_HUNTER.md)
- [安全应用指南](SECURITY_APPLICATIONS.md)

## ✨ 主要功能

- 🕷️ **深度网页爬虫** - 智能爬取网站页面，支持JavaScript渲染
- 🔍 **API自动发现** - 从HTML、JavaScript、表单中提取API端点
- 📊 **多格式导出** - 支持JSON、CSV、Markdown、HTML格式导出
- 🌐 **Web管理界面** - 直观的Web界面管理扫描会话和结果
- 🔧 **灵活配置** - 丰富的配置选项，适应不同扫描需求
- 📈 **统计分析** - 详细的扫描统计和API分类分析

## 🛡️ 安全应用场景

### 渗透测试
- **攻击面发现**: 快速识别所有可能的攻击入口
- **漏洞测试**: 针对发现的API进行安全测试
- **权限测试**: 测试API的认证和授权机制

### 红队演练  
- **侦察阶段**: 快速了解目标系统的API架构
- **攻击路径**: 基于API发现制定攻击策略
- **持久化**: 在API层面建立访问后门

### 蓝队防御
- **资产清单**: 全面了解组织的API暴露面
- **风险评估**: 识别高风险的API端点
- **防御策略**: 基于实际暴露面制定防护措施

### 合规审计
- **数据保护**: 识别处理敏感数据的API
- **访问控制**: 检查API的权限控制机制
- **审计日志**: 确保API访问的可追溯性

## 🔒 安全价值

API Hunter在网络安全领域具有重要价值：

### 真实测试验证
我们使用FOFA API对98个真实网站进行了测试：
- ✅ 成功发现16个API端点
- ✅ 包括企业管理系统API、Swagger文档、认证接口
- ✅ 发现了完整的业务系统API架构

### 安全应用场景
- **渗透测试**: 快速发现攻击面，提升测试效率10倍
- **红队演练**: 自动化侦察，快速了解目标系统架构  
- **蓝队防御**: 全面的API资产清单，制定针对性防护
- **合规审计**: 自动化API安全合规检查

**详细了解安全应用**: [安全应用指南](SECURITY_APPLICATIONS.md)

## 🚀 快速开始

### 安装
   ```bash
   # 下载对应平台的版本
   wget https://github.com/your-repo/api-hunter/releases/latest/api-hunter-linux-amd64
   chmod +x api-hunter-linux-amd64
   mv api-hunter-linux-amd64 api-hunter
   ```

2. **从源码构建**
   ```bash
   git clone https://github.com/your-repo/api-hunter.git
   cd api-hunter
   ./build.sh
   ```

### 基本使用

1. **扫描网站**
   ```bash
   ./api-hunter scan -u https://example.com
   ```

2. **启动Web界面**
   ```bash
   ./api-hunter web
   ```
   然后访问 http://localhost:8080

3. **导出结果**
   ```bash
   ./api-hunter export -s session_id -f json -o results.json
   ```

## 📖 详细使用说明

### 命令行界面

#### 扫描命令
```bash
# 基本扫描
./api-hunter scan -u https://example.com

# 指定扫描深度和并发数
./api-hunter scan -u https://example.com -d 3 -w 5

# 指定会话ID
./api-hunter scan -u https://example.com -s my_scan_session
```

#### Web界面
```bash
# 启动Web服务器
./api-hunter web

# 指定端口
./api-hunter web -p 9090
```

#### 导出数据
```bash
# 导出为JSON格式
./api-hunter export -s session_id -f json -o results.json

# 导出为CSV格式
./api-hunter export -s session_id -f csv -o results.csv

# 导出为HTML报告
./api-hunter export -s session_id -f html -o report.html
```

#### JavaScript分析
```bash
# 分析JavaScript文件中的API调用
./api-hunter analyze -s session_id
```

#### 统计信息
```bash
# 查看扫描统计
./api-hunter stats -s session_id

# 查看所有会话
./api-hunter stats
```

### 配置文件

API Hunter 使用 YAML 格式的配置文件 `config.yaml`：

```yaml
# 爬虫配置
crawler:
  max_workers: 10          # 最大并发数
  delay: 1s               # 请求间隔
  timeout: 30s            # 请求超时
  max_depth: 5            # 最大爬取深度
  max_pages: 1000         # 最大页面数
  use_headless: false     # 是否使用无头浏览器

# 数据库配置
database:
  type: "sqlite"          # 数据库类型
  sqlite_path: "./data/api_hunter.db"

# Web界面配置
web:
  port: 8080
  host: "0.0.0.0"
```

完整配置选项请参考 `config.yaml` 文件。

## 🔧 高级功能

### 1. JavaScript分析

API Hunter 可以分析JavaScript文件中的API调用：

- **Fetch API** - `fetch()` 调用
- **Axios** - axios HTTP客户端
- **jQuery AJAX** - `$.ajax()`, `$.get()`, `$.post()` 等
- **XMLHttpRequest** - 原生XHR调用
- **WebSocket** - WebSocket连接

### 2. API类型识别

自动识别不同类型的API：

- **REST API** - 标准的RESTful接口
- **GraphQL** - GraphQL查询端点
- **WebSocket** - WebSocket连接
- **JSON API** - JSON格式的API端点

### 3. 智能过滤

支持多种过滤选项：

- **域名过滤** - 允许/禁止特定域名
- **路径过滤** - 过滤特定路径模式
- **文件类型过滤** - 跳过图片、CSS等静态资源

### 4. 数据导出

支持多种导出格式：

- **JSON** - 结构化数据，便于程序处理
- **CSV** - 表格格式，便于Excel分析
- **Markdown** - 文档格式，便于阅读
- **HTML** - 网页报告，包含样式和交互

## 🌐 Web界面功能

### 仪表板
- 📊 扫描统计概览
- 📈 API发现趋势
- 🔗 最新发现的API

### 会话管理
- 📋 查看所有扫描会话
- 🗑️ 删除不需要的会话
- 📊 会话详细统计

### API列表
- 🔍 搜索和过滤API
- 📝 查看API详细信息
- 🏷️ 按类型和域名分类

### 页面管理
- 📄 查看已爬取的页面
- 📊 页面统计信息
- 🔗 页面链接关系

## 🛠️ 开发指南

### 项目结构
```
api-hunter/
├── main.go              # 主程序入口
├── config.yaml          # 配置文件
├── storage/             # 数据存储层
│   ├── models.go        # 数据模型
│   ├── database.go      # 数据库操作
│   └── export.go        # 数据导出
├── crawler/             # 爬虫引擎
│   ├── spider.go        # 爬虫核心
│   ├── parser.go        # 页面解析
│   └── detector.go      # API检测
├── analyzer/            # 分析器
│   ├── javascript.go    # JS分析
│   ├── network.go       # 网络分析
│   └── pattern.go       # 模式匹配
├── utils/               # 工具函数
│   ├── http.go          # HTTP工具
│   ├── url.go           # URL处理
│   └── filter.go        # 过滤器
└── web/                 # Web界面
    ├── server.go        # Web服务器
    ├── static/          # 静态资源
    └── templates/       # HTML模板
```

### 构建和测试
```bash
# 安装依赖
go mod tidy

# 运行测试
go test ./...

# 构建项目
./build.sh

# 运行开发版本
go run main.go scan -u https://example.com
```

## 📊 使用示例

### 示例1：扫描电商网站API
```bash
# 扫描淘宝首页API
./api-hunter scan -u https://www.taobao.com -d 2 -w 3

# 启动Web界面查看结果
./api-hunter web

# 导出API列表
./api-hunter export -s scan_1640995200 -f json -o taobao_apis.json
```

### 示例2：分析单页应用
```bash
# 使用无头浏览器扫描SPA应用
./api-hunter scan -u https://spa-app.com --use-headless

# 分析JavaScript文件
./api-hunter analyze -s session_id

# 导出HTML报告
./api-hunter export -s session_id -f html -o spa_report.html
```

## 🔒 安全说明

- API Hunter 仅用于合法的安全测试和API发现
- 请遵守目标网站的robots.txt和使用条款
- 建议在测试环境中使用，避免对生产环境造成影响
- 扫描时请控制并发数和请求频率

## 🤝 贡献指南

欢迎提交Issue和Pull Request！

1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 打开Pull Request

## 📄 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。

## 🙏 致谢

- [Gin](https://github.com/gin-gonic/gin) - Web框架
- [GORM](https://github.com/go-gorm/gorm) - ORM库
- [Goquery](https://github.com/PuerkitoBio/goquery) - HTML解析
- [Chromedp](https://github.com/chromedp/chromedp) - 无头浏览器
- [Cobra](https://github.com/spf13/cobra) - CLI框架

## 📞 联系方式

- 项目主页: https://github.com/your-repo/api-hunter
- 问题反馈: https://github.com/your-repo/api-hunter/issues
- 邮箱: your-email@example.com

---

**API Hunter** - 让API发现变得简单高效！ 🚀